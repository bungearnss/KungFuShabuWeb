/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  reactStrictMode: false,
  images: {
    formats: ['image/webp'],
  },
}

module.exports = nextConfig
